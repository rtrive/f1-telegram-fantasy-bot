class User:
    def __init__(self, username: str, user_id: str, global_id: str, external_id: str):
        self.username = username
        self.user_id = user_id
        self.global_id = global_id
        self.external_id = external_id
